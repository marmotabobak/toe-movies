{
  "name": "tvoe-movies",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"uvicorn backend.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
    "import": "python -m backend.scripts.import_data",
    "build": "npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
